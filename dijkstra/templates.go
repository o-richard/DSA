package dijkstra

var Templates = map[string]string{
	"dijkstra": `package dijkstra

// Returns the destinations and the respective distances
func Dijkstra(graph map[string]map[string]int, source string) map[string]int {
	// Edit here
}
	`,
}
